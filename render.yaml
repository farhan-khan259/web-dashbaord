services:
  - type: web_service
    name: ewb-dashboard-frontend
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: EWB_API_BASE_URL
        value: https://ewb-dashboard-backend.onrender.com
